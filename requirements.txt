# torch/torchmetrics use >= rather than == on purpose: this studio's actual
# installed torch is a CUDA-specific build (2.7.0+cu128). A strict ==2.7.0
# pin would make `pip install -r requirements.txt` try to replace that
# working GPU build with a plain CPU wheel, since pip treats the local
# version suffix (+cu128) as significant for exact-equality matching.
lightning==2.5.2
lightning_sdk==2026.1.30
litdata==0.2.45
litlogger==0.1.7
litmodels==0.1.8
litserve==0.2.12
torch>=2.7.0
torchmetrics>=1.3.1
pandas==2.1.4
matplotlib==3.8.2
streamlit==1.27.2
requests==2.32.4
